import { Hay } from './hay';
export interface HayCommandInstance {
    run(): Promise<any>;
    watch?(): Promise<void>;
}
export interface HayCommand {
    new (hay: Hay): HayCommandInstance;
    options?: Function;
}
