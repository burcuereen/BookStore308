import { BaseBuilder } from './base';
import { File } from '../template';
import { Hay } from '../hay';
export declare class LayoutsBuilder extends BaseBuilder {
    template: string;
    constructor(hay: Hay);
    parseFile(file: File): Promise<any>;
    removeFile(file: File): Promise<any>;
    run(): Promise<any>;
    finish(): void;
}
