import { BaseBuilder } from './base';
import { File } from '../template';
import { Hay } from '../hay';
export declare class CopyBuilder extends BaseBuilder {
    template: string;
    constructor(hay: Hay);
    loadOtherFiles(): Promise<string[]>;
    parseOtherFile(file: string): Promise<any>;
    removeFile(file: File): Promise<any>;
    run(): Promise<any>;
    finish(): void;
}
