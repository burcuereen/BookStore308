import { Hay } from '../hay';
import { File } from '../template';
import { ProgressBar } from '../reporter';
export declare type BuilderConfig = {
    directory: string;
    fileExtensions: string[];
    name: string;
};
export declare abstract class BaseBuilder {
    hay: Hay;
    config: BuilderConfig;
    progressBar: ProgressBar;
    WATCH_INITIATED: boolean;
    constructor(hay: Hay);
    abstract finish(): void;
    loadFile(fileName: string): Promise<File>;
    loadFiles(): Promise<File[]>;
    abstract run(): any;
    setConfig(config: BuilderConfig): void;
    private checkExtensions(extensions);
    watch?(): Promise<void>;
}
